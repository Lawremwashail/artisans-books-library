<nav class="bg-gray-800 text-white px-6 py-3 flex justify-between items-center">
  <!-- Left Side - Brand -->
  <div class="text-lg font-semibold">
    <%= link_to "Online Library", root_path, class: "hover:text-gray-300" %>
  </div>

  <!-- Right Side - Links -->
  <div class="flex space-x-4">
    <% if session[:user_id] %>
      <%= link_to "Home", root_path, class: "hover:text-gray-300" %>
      <%= link_to "Books", books_path, class: "hover:text-gray-300" %>
      <%= link_to "Profile", profile_path, class: "hover:text-gray-300" %>
      <%= button_to "Logout", logout_path, method: :delete, class: "bg-red-500 px-3 py-1 rounded-md text-white hover:bg-red-600" %>
    <% else %>
      <%= link_to "Sign Up", signup_path, class: "hover:text-gray-300" %>
      <%= link_to "Login", login_path, class: "hover:text-gray-300" %>
    <% end %>
  </div>
</nav>
