<nav class="fixed top-0 left-0 w-full bg-blue-600 text-white shadow-md z-50">
  <div class="max-w-6xl mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <!-- Logo -->
      <a href="/" class="text-xl font-bold">Library</a>

      <!-- Navigation Links -->
      <div class="space-x-6">
        <a href="/" class="hover:underline <%= 'underline font-semibold' if current_page?(root_path) %>">Home</a>
        <a href="/books" class="hover:underline <%= 'underline font-semibold' if current_page?(books_path) %>">Books</a>
        <a href="/profile" class="hover:underline <%= 'underline font-semibold' if current_page?(profile_path) %>">Profile</a>
        <a href="/logout" class="hover:underline">Logout</a>
      </div>
    </div>
  </div>
</nav>

<!-- Add padding so content is not hidden behind navbar -->
<div class="pt-16">
